import { FC } from "react";
import { BrowserRouter, Routes } from "react-router-dom";

const App: FC = () => {
return (
    <BrowserRouter>
      <Routes>
      </Routes>
    </BrowserRouter>
  );
};

export default App;
